#include "main.h"

/**
 * _strlen - return the length of a string
 * @s: char type pointer
 * Return: length of string
 */
int _strlen(char *s)
{
	int c;

	for (c = o; s[c] != '\0'; c++)
		;
	return (c);
}

